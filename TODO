gfx.c:
  * occasionally a counter-drop animation doesn't look smooth.
    Haven't yet figured out what causes this.

prefs.c:
  * the keyboard control entries are silly - typing certain keys
    (eg. TAB and the cursor keys) changes the focus.

